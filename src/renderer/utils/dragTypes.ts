/**
 * Shared HTML5 drag-and-drop MIME type for dragging a player roster
 * Pokemon (TeamRosterColumn.tsx) onto an empty Battlefield slot
 * (Battlefield.tsx) - centralized so both sides of the drag/drop pair
 * agree on the exact string.
 */
export const PLAYER_POKEMON_DRAG_TYPE = 'application/x-choicebuds-pokemon';
